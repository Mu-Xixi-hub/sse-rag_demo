spring:
  application:
    name: sse-rag-demo

  threads:
    virtual:
      enabled: true  # 虚拟线程启动


  ai:
    openai:
      api-key: XXXXX
      base-url: https://api.deepseek.com
      chat:
        options:
          model: deepseek-chat
          temperature: 0.7

server:
  port: 8080